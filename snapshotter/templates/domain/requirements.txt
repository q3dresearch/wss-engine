# This repo pins one engine version; the workflows install the same pin
# (resolving the owner as ${{ github.repository_owner }}). For local
# development against a sibling checkout: pip install -e ../snapshotter
snapshotter @ git+https://github.com/{{OWNER}}/snapshotter.git@v{{ENGINE_VERSION}}
